A simple Restful web service using Java 8, Maven and JAX-RS 2.7

1. mvn clean install

To Create a link for Personal repo in Mac if you are already having any official Repo:

cd /Users/<username>/.ssh
touch config
nano config
vi config
ssh-add -D
eval `ssh-agent -s`
eval `ssh-agent -s`
ssh-add -l
ssh-add ~/.ssh/id_rsa
ssh-add ~/.ssh/id_rsapers
ls -lrt
cat config
ssh -T git@github.com
ssh -T git@github.company.com






#git push -u origin master



git init
git add .
git commit -m "SampleCommit"


Create a repo and then follow the commend given after the creation

git remote add origin git@github.com:SatyanandPathak/SpringBootService.git


git push -u origin master


git@github.com:SatyanandPathak/Angular4.git

